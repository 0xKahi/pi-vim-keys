import type { KeyId } from '@earendil-works/pi-tui';

export const EXTENSION_ID = 'pi-vim-keys';
export const DEFAULT_LEADER_KEY: KeyId = 'space';
export const DEFAULT_KEY_TIMEOUT = 50; // in milliseconds

export const COLOR_HEX_REGEX = /^#[0-9a-fA-F]{6}$/;
