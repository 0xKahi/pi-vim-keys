{
  "name": "@0xkahi/pi-vim-keys",
  "version": "0.0.0",
  "description": "Vim style input editor for Pi",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "vim-key-bindings"
  ],
  "author": "0xKahi",
  "repository": {
    "type": "git",
    "url": "https://github.com/0xKahi/pi-vim-keys.git"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "biome": "biome check ./src ./test",
    "lint": "bun biome",
    "lint:fix": "bun biome --write",
    "type-check": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run lint:fix && bun run type-check",
    "release": "changeset publish",
    "version": "changeset version",
    "changeset": "changeset",
    "buildSchema": "bun run scripts/build-schema.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@changesets/cli": "^2.31.0",
    "@types/node": "^24.0.0",
    "bun-types": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.4.3",
    "@earendil-works/pi-ai": "^0.78.1",
    "@earendil-works/pi-coding-agent": "^0.78.1",
    "@earendil-works/pi-tui": "^0.78.1"
  }
}
